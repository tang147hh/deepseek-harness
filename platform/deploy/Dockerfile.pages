FROM node:22-alpine

WORKDIR /app
COPY pages/package.json pages/package-lock.json ./
RUN npm ci --omit=dev

COPY pages/src ./src

ENV NODE_ENV=production \
    HOST=0.0.0.0 \
    PORT=8080

USER node
EXPOSE 8080

CMD ["node", "src/server.js"]
